{
  "service": "user-service",
  "version": "1.0.0",
  "database": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "name": "${DB_NAME}"
  }
} 